meta {
  name: Login (Admin)
  type: http
  seq: 6
}

post {
  url: http://localhost:8080/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@linsi.com",
    "password": "admin"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
